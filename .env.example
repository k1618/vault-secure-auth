# Copia este archivo a .env y ajusta los valores.
# NUNCA subas tu .env real a GitHub (ya está listado en .gitignore).

SECRET_KEY=cambia-esto-por-una-llave-larga-y-aleatoria
FLASK_ENV=development
FLASK_DEBUG=1

# Solo se usa en producción; en desarrollo se usa SQLite local por default.
DATABASE_URL=
